using System;
using System.ComponentModel;

namespace SOP_RSA_Program
{
    class Program
    {
        // Creates byte arrays to hold the original, encrypted, and decrypted data.
        private byte[] dataToEncrypt;
        private byte[] dataEncrypted;
        private byte[] dataDecrypted;
        
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
        }
    }
}